<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jk51.modules.faceplusplus.mapper.DetectMapper">

    <insert id="detectLog">
        insert into b_detect_log (site_id,param,result,remark) values (#{siteId},#{param,jdbcType=VARCHAR},#{result,jdbcType=VARCHAR},#{remark,jdbcType=VARCHAR})
    </insert>

</mapper>

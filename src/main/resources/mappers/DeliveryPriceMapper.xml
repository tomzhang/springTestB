<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk51.modules.trades.mapper.DeliverypriceMapper">


    <select id="getPrice" parameterType="map" resultType="int">
        SELECT price FROM b_delivery_price WHERE type1=#{type1} AND type2=#{type2} AND type3=#{type3}
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk51.modules.merchant.mapper.SignContractMapper">

    <select id="getById" resultType="com.jk51.model.merchant.SignContract">
        select id,contract_contents,commission,sms_fees from sign_contract where id=#{id}
    </select>
</mapper>
